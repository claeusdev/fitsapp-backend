<div class="container">
  <div class="inbox-wrapper">

    <%= link_to "New Chat", new_message_path, class: "btn btn-secondary" %>
    <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn  btn-secondary" %>
  <h1>Your messages</h1>
  <table class="table">
    <thead>
      <tr>
        <th>From</th>
        <th>Message</th>
        <th>Recieved at</th>
      </tr>
    </thead>
    <tbody>
      <% if @messages.count > 0 %>
        <%= render @messages %>
      <% else %>
        <tr>
          <td><p>You currently have no messages</p></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
